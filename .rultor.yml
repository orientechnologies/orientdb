docker:
  as_root: true
  image: yegor256/java8

decrypt:
  settings.xml: "repo/settings.xml.asc"
  pubring.gpg: "repo/pubring.gpg.asc"
  secring.gpg: "repo/secring.gpg.asc"

release:
  commanders:
    - mpollmeier
    - velo
  script: |
    mvn clean deploy -f driver/pom.xml -DskipTests -B --settings /home/r/settings.xml
