export class OrientGraph {

  constructor() {

  }

  create(element, config, metadata, vertexActions, edgeActions) {
    alert("Dajeeee!");
    // return new OGraph(elementId, config, metadata, vertexActions, edgeActions);
  }
}
