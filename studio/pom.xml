<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.orientechnologies</groupId>
        <artifactId>orientdb-parent</artifactId>
        <version>4.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>
	<artifactId>orientdb-studio</artifactId>
	<name>OrientDB Studio</name>
	<description>OrientDB Studio - web interface for OrientDB server</description>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
				<executions>
					<execution>
						<id>unzip-community</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>unpack</goal>
						</goals>
						<configuration>
							<skip>${skipTests}</skip>
							<outputDirectory>${project.build.directory}</outputDirectory>
							<stripVersion>true</stripVersion>
							<artifactItems>
								<artifactItem>
									<groupId>com.orientechnologies</groupId>
									<artifactId>orientdb-community</artifactId>
									<version>${project.version}</version>
									<overWrite>true</overWrite>
									<type>zip</type>
								</artifactItem>
							</artifactItems>
							<excludes>**/*orientdb-studio-*.zip</excludes>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<executions>
					<execution>
						<id>copy-studio</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>
								${project.build.directory}/orientdb-community-${project.version}/plugins
              </outputDirectory>
							<resources>
								<resource>
									<directory>${project.build.directory}</directory>
									<includes>
										<include>
											orientdb-studio-${project.version}.zip</include>
									</includes>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Build the front end -->
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>exec-maven-plugin</artifactId>
				<version>3.6.3</version>
				<executions>
					<execution>
						<phase>generate-resources</phase>
						<goals>
							<goal>exec</goal>
						</goals>
					</execution>
					<execution>
						<id>Install Deps and Dist</id>
						<phase>generate-resources</phase>
						<goals>
							<goal>exec</goal>
						</goals>
						<configuration>
							<executable>yarn</executable>
							<arguments>
								<argument>run</argument>
								<argument>dist</argument>
							</arguments>
							<workingDirectory>.</workingDirectory>
						</configuration>
					</execution>
<!--
					<execution>
						<id>Run OrientB</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>pre-integration-test</phase>
						<configuration>
							<skip>${skipTests}</skip>
							<executable>test/scripts/run.sh</executable>
							<arguments>
								<argument>
									${project.build.directory}/orientdb-community-${project.version}/bin/server.sh</argument>
								<argument>-DORIENTDB_ROOT_PASSWORD=root</argument>
							</arguments>
						</configuration>
					</execution>
					<execution>
						<id>Stop OrientB</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>post-integration-test</phase>
						<configuration>
							<skip>${skipTests}</skip>
							<executable>test/scripts/stop.sh</executable>
							<arguments>
								<argument>
									${project.build.directory}/orientdb-community-${project.version}/bin/shutdown.sh</argument>
							</arguments>
						</configuration>
					</execution>
					<execution>
						<id>npm run test (test)</id>
						<goals>
							<goal>exec</goal>
						</goals>
						<phase>integration-test</phase>
						<configuration>
							<skip>${skipTests}</skip>
							<executable>test/scripts/test.sh</executable>
						</configuration>
					</execution>
-->
				</executions>
				<configuration>
					<executable>npm</executable>
					<arguments>
						<argument>-version</argument>
					</arguments>
					<workingDirectory>.</workingDirectory>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-assembly-plugin</artifactId>
				<!--<version>2.4</version>-->
				<executions>
					<execution>
						<id>distribution-package</id>
						<phase>package</phase>
						<goals>
							<goal>single</goal>
						</goals>
						<configuration>
							<appendAssemblyId>false</appendAssemblyId>
							<descriptors>
								<descriptor>${basedir}/assembly/archive.xml</descriptor>
							</descriptors>
							<filters>
								<filter />
							</filters>
							<ignoreDirFormatExtensions>false</ignoreDirFormatExtensions>
							<tarLongFileMode>gnu</tarLongFileMode>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-release-plugin</artifactId>
				<configuration>
					<pushChanges>false</pushChanges>
					<tagNameFormat>@{project.version}</tagNameFormat>
					<scmCommentPrefix>OrientDB</scmCommentPrefix>
					<localCheckout>true</localCheckout>
				</configuration>
			</plugin>
		</plugins>
	</build>
</project>
